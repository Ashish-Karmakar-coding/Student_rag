/**
 * packages/shared/src/index.ts
 *
 * Single entry point — re-exports everything from types.ts and schemas.ts.
 * Import in backend: import { ... } from "@study-tutor/shared"
 * Import in frontend: import { ... } from "@study-tutor/shared"
 */

export * from "./types";
export * from "./schemas";
